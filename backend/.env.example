# Optional override for matchup probability table
# MATCHUP_CSV_PATH=./data/nation_matchup_probabilities_proxy_logreg_ALL_pairs_calibrated.csv

# Optional CORS config for deployed frontend hosts
# CORS_ALLOW_ORIGINS=https://your-frontend.example.com
# CORS_ALLOW_ORIGIN_REGEX=^https://([a-z0-9-]+)\.your-frontend\.example\.com$
